# DeclarativePipeline_Python_Sales_Project
Create declarative pipeline using python code. There are three layers like bronze,silver and gold. We are using sql tables as sources . When extract the tables data we cleansing and applied business rule then only loaded data into that concern data layers. Finally, creates materialized tables and applied SCD-1 AND SCD-2 type dimensions.
